import { createGlobalStyle } from 'styled-components';
import { typographyStyles } from '../utils/typography';

const TypographyBase = createGlobalStyle`${typographyStyles}`;

export default TypographyBase;
